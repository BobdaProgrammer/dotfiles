package huffman

import (
	//"os"
	//"bufio"
	//"log"
)

type HuffmanNode struct{
	Val int
	Left *HuffmanNode
	Right *HuffmanNode
}

func main(){
	/*fileName:="data.txt"
	file, err:=os.Open(fileName)

	if err!=nil{
		log.Fatal("Couldn't open data")
	}
	scanner:=bufio.NewScanner(file)
	for line := range scanner{
		
	}*/
	data:="AAABBBBCCCCCCCCDDD"
}
